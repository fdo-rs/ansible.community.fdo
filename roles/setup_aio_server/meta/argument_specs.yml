argument_specs:
  main:
    short_description: Setup AIO server
    description:
      - Checks RHEL subscription, installs fdo-admin-cli package with dnf, starts and enables the FDO AIO service
        and then opens the required ports.
      
  
